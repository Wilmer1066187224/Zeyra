<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{{ \$invoice->name }}</title>
    <style>
        body {
            font-family: 'DejaVu Sans', sans-serif;
            background-color: #f9fafb;
            color: #1f2937;
            font-size: 10px;
            margin: 36pt;
        }

        .header {
            background-color: #1e3a8a;
            color: white;
            padding: 15px;
            text-align: center;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .header h1 {
            margin: 0;
            font-size: 22px;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 1rem;
        }

        .table th, .table td {
            padding: 10px;
            border: 1px solid #d1d5db;
        }

        .table thead {
            background-color: #3b82f6;
            color: white;
        }

        .text-right {
            text-align: right;
        }

        .total {
            background-color: #facc15;
            font-weight: bold;
            padding: 10px;
            text-align: right;
            border-radius: 4px;
        }

        .footer {
            font-size: 9px;
            color: #6b7280;
            text-align: center;
            margin-top: 30px;
        }

        .section {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Factura de Venta</h1>
    </div>

    <table class="table">
        <tr>
            <td><strong>Número:</strong> {{ \$invoice->getSerialNumber() }}</td>
            <td><strong>Fecha:</strong> {{ \$invoice->getDate() }}</td>
        </tr>
        <tr>
            <td><strong>Vendedor:</strong> {{ \$invoice->seller->name }}</td>
            <td><strong>Cliente:</strong> {{ \$invoice->buyer->name }}</td>
        </tr>
    </table>

    <div class="section">
        <table class="table">
            <thead>
                <tr>
                    <th>Producto</th>
                    <th>Cantidad</th>
                    <th>Precio Unitario</th>
                    <th>Subtotal</th>
                </tr>
            </thead>
            <tbody>
                @foreach(\$invoice->items as \$item)
                <tr>
                    <td>{{ \$item->title }}</td>
                    <td class="text-right">{{ \$item->quantity }}</td>
                    <td class="text-right">{{ \$invoice->formatCurrency(\$item->price_per_unit) }}</td>
                    <td class="text-right">{{ \$invoice->formatCurrency(\$item->sub_total_price) }}</td>
                </tr>
                @endforeach
            </tbody>
        </table>
    </div>

    <div class="section text-right">
        <p class="total">TOTAL: {{ \$invoice->formatCurrency(\$invoice->total_amount) }}</p>
    </div>

    <div class="footer">
        <p>Gracias por su compra.</p>
        <p>{{ __('invoices::invoice.amount_in_words') }}: {{ \$invoice->getTotalAmountInWords() }}</p>
    </div>

    <script type="text/php">
        if (isset(\$pdf) && \$PAGE_COUNT > 1) {
            \$text = "{{ __('invoices::invoice.page') }} {PAGE_NUM} / {PAGE_COUNT}";
            \$size = 10;
            \$font = \$fontMetrics->getFont("Verdana");
            \$width = \$fontMetrics->get_text_width(\$text, \$font, \$size) / 2;
            \$x = (\$pdf->get_width() - \$width);
            \$y = \$pdf->get_height() - 35;
            \$pdf->page_text(\$x, \$y, \$text, \$font, \$size);
        }
    </script>
</body>
</html>
